<Window x:Class="Article_Graph_Analysis_Application.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Article Graph Analysis"
        Height="800"
        Width="1400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" x:Name="GraphHost"/>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1,0,0,0" Padding="10">
            <StackPanel x:Name="RightPanel"/>
        </Border>
    </Grid>
</Window>